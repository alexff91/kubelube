## What does this PR do?

<!-- A short description of the change and the motivation behind it. -->

## How was it tested?

<!-- e.g. against a kind cluster, with `npm run dev`, … -->

## Checklist

- [ ] `npm run format:check`, `npm run lint`, `npm run typecheck` and `npm run build` pass
- [ ] The renderer stays sandboxed (no Node/Electron imports under `src/renderer`)
- [ ] Commit messages follow Conventional Commits
